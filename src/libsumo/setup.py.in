from setuptools import setup, find_packages
import os

setup(name='libsumo',
      version='${PACKAGE_VERSION}',
      packages=find_packages(),
      package_data = {'libsumo': ['*.pyd', '*.so']}
      )
