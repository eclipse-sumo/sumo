# tests for rail signals
rail_signal

# trams driving in a loop servicing stops
tramwayLoop

# splitting and joining train parts
portion_working

# test rail-specific routing features (train reversal / train length constraints)
routing

# tests for train reversal
reversal

# Two trains approaching conflicting block simultaneously causing a head-on conflict
headonconflict

# Tests interlocked block signalling
interlocking

# speed when passing tracks with different speed limits
slow_fast_slow

# trains passing each other at two loops
two_passing_loops

# as above but with intermediate nodes on the tracks
two_passing_loops2

# train insertion should try to prevent a frontal collision
bidi_conflict_insertion

# train insertion should try to prevent a frontal collision (example where insertion is safe)
bidi_noconflict_insertion

# train insertion should try to prevent a frontal collision (example where insertion is safe)
bidi_noconflict_insertion2

# train insertion should try to prevent a frontal collision (example where insertion is safe)
bidi_noconflict_insertion3

# train insertion should try to prevent a frontal collision (example where insertion is unsafe)
bidi_conflict_parkingInsertion
crossing_tracks

# rerouting trains that pass each other in a complex network
alternativeRoute

# a train that circles endlessly, switches can be controlled in sumo-gui
model_railroad

# load a rail_signal where all connections are marked as uncontrolled
uncontrolled_links

# railway-specific bugs
bugs

# railway-crossings with trafficlights
crossing

# test for various types of train collision
collision
