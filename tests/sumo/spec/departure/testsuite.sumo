departLane
departPos
departSpeed

# tests for starting with an offset along the route
departEdge

# one vehicle approaching a lane with a lower max speed
approach_slow_lane

# unknown lane
# lane not in edge
# position larger than lane length (positive/negative)
# position not numeric
# speed negative
# speed larger than vehicle's max
# speed larger than lane's max

# test option --random-depart-offset
depart_random

# test option --extrapolate-departpos
extrapolate_departpos

# test insertion on short edge (#10298)
extrapolate_departpos_short

# checks using departPosLat with the default model
posLat_beyondLane

# Errors on emission
errors

# tests selective removal of safety checks during insertion
insertionChecks
