# the faster detour is jammed
jam_default

# the faster detour is jammed
jam_reroutingDevice

# the faster detour is jammed
jam_reroutingMode9

# the faster detour is jammed
jam_reroutingMode12

# the slower detour has higher priority
prioFactor_default

# the slower detour has higher priority
prioFactor_routingDevice

# the slower detour has higher priority
prioFactor_routingMode9

# the slower detour has higher priority
prioFactor_routingMode12

# the slower detour has higher routing preferences
pref_default

# the slower detour has higher routing preferences
pref_routingDevice

# the slower detour has higher routing preferences
pref_routingMode9

# the slower detour has higher routing preferences
pref_routingMode12
