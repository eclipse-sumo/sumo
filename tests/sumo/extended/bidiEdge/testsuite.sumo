# tests for a two-way-left-turn lane
TWLT

# small extract from the dorpfeldstr. scenario (https://github.com/DLR-TS/sumo-scenarios/tree/main/Doerpfeldstr)
Doerpfeldstr

# one pair of bidi-edges (between normal edges) and 2 vehicles
1edge_2vehicles

# one pair of bidi-edges (between normal edges) and 2 vehicles (right vehicle goes first)
1edge_2vehicles2

# one pair of bidi-edges (between normal edges) and 2 vehicles
1edge_2vehicles_sameDir

# one pair of bidi-edges (between normal edges) and 2 vehicles entering at the same time
1edge_2vehicles_sameTime

# one pair of bidi-edges (between normal edges) and 2 vehicles entering at the same time
1edge_2vehicles_sameTime_prioLeft

# one pair of bidi-edges (between normal edges) and 2 vehicles entering at the same time
1edge_2vehicles_sameTime_prioRight

# two consecutive pairs of bidi-edges (between normal edges) and 2 vehicles
2edges_between

# bidi edge where not all lanes have a bidilane
multilane

# load curved edge with shared median lane
# (test/netconvert/import/XML/bidi/curved)
curved
