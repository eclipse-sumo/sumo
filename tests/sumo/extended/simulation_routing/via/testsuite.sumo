# trip with via
trips

# trip with via taz
trips_taz

# trip with via and periodic rerouting
trips_periodic

# trip with via and periodic rerouting
trips_periodic2

# trip with via and periodic rerouting
trips_periodic2_ignore

# inefficient route with via
flow

# inefficient route with via (as child element of flow)
flow_embedded

# inefficient route with via
route

# inefficient route with via (as child element of the vehicle)
route_embedded
