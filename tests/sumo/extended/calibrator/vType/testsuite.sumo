# test calibrator flow with only a type value given
calibrate_type

# test calibrator flow with only a type value given
calibrate_type_junction

# test calibrator flow with only a type value given
calibrate_type_junction_local

# map from one type distribution to another.
calibrate_type_distribution

# calibrate type and speed at the same time
type_and_speed

# only calibrate a specific vehicle type
vTypes

# new type has a wider and shifted speedFactor distribution
change_speedFactor

# original type had speedDev 0 but new type has not
change_speedFactor_origDev0
