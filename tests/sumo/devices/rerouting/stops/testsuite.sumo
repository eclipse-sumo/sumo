# Tests whether cyclical stops are handled correctly while reroutig
cyclic_trip

# reroute while on a stop edge but already past the stop
cyclic_trip_past_stop

# define a repeating route without defining edges
public_transport_implicit_route
