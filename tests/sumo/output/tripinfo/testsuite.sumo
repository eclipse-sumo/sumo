# no traffic, just plain output
no_vehicles

# one vehicle is moving
one_vehicle

# one vehicle is moving, subsecond
one_vehicle_subsecond

# two vehicles are moving which have same properties
two_same_vehicles

# two vehicles are moving; the first runs with 10m/s, the second with 9m/s
two_vehicles_diff_speed

# two vehicles are moving; the first is 5m long, the second 15m
two_vehicles_diff_length

# teleporting a vehicle
teleport

# teleporting over multiple edges
teleport_multihop

# write output for departed vehicles which have not arrived at simulation end
write_unfinished

# write output for departed persons which have not arrived at simulation end
write_unfinished_person

# count length of internal lanes as well
internal_lanes

# compute correct departDelay for triggered vehicle
depart_triggered

# compute correct departDelay for triggered vehicle (insertion is delayed due to a blocking vehicle)
depart_triggered_delayed

# disable tripinfo output by default and enable it for selected vType
enable_by_type

# Testing error cases
errors
