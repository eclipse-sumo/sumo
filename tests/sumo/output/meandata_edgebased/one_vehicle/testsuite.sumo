# One output; sampling at 1s
interval1s

# One output; sampling at 1s
interval1s_aggregate

# One output; sampling at 15s
interval15s

# One output; sampling at 60s
interval60s

# Two outputs; one sampling at 15s, one at 60s
interval15s60s

# filter written attributes
writeAttributes

# filter written attributes
writeAttributes_invalid

# set option to define edgeData without additional file
via_option
