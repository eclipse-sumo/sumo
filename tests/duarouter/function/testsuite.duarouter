# go on even if the route is defect
ignore_errors

# keep valid input and remove invalid
ignore_errors2

# loaded route with disallowed edges
ignore_errors3
ignore_errors_invalid_depart_edge

# randomize edge weights
random-factor

# write costs
costs

# Testing loop removal
remove_loops

# Testing setting depart/arrival defaults
defaults

# Tests whether the end-option is used properly
endstep

# Tests whether using other measures works
measure

# shortest path finding related tests
routing
repair

# test option --skip-new-routes
skip_routing

# keep some or all of the old routes
keep_routes

# test options that affect estimated travel times
penalty
