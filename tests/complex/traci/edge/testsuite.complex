# Tests the edge API
edge

# see #14552
setMaxSpeed

# retrieve bidi lane
bidi

# see #16422
subscribeContextVehicles

# regression: permanent TraCI permission changes (setDisallowed) must
# invalidate the CCH free-flow metric so rerouteTraveltime avoids the edge
permissions_cch_reroute
