# Test whether incrementally changing the lateral position (by 0.2m) to left lane with the sublane model causes lateral collision
# Collision warning should be returned
leftLane0.2

# Test whether incrementally changing the lateral position (by 0.5m) to left lane with the sublane model causes lateral collision
# Collision warning should be returned
leftLane0.5

# Test whether incrementally changing the lateral position (by 0.2m) to right lane with the sublane model causes lateral collision
# Collision warning should be returned
rightLane0.2

# Test whether incrementally changing the lateral position (by 0.5m) to right lane with the sublane model causes lateral collision
# Collision warning should be returned
rightLane0.5
