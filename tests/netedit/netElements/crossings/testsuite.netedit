# create crossing
create

# create crossing in a junction with sidewalks 
create_sidewalks

# delete crossing
delete

# delete junction, undo and redo (net must be the same as original)
delete_junction

# delete junction, undo, but not redo
delete_junction_noredo

# delete crossing, undo, but not redo
delete_noredo

# delete junction with TLS
delete_TLS

# inspect crossing
inspect

# load big net with crossigns
load_big

# load small net with crossings
load_small
