# create crossing
create

# create crossing in a junction with sidewalks 
create_sidewalks

# delete crossing
delete

# delete crossing, undo, but not redo (net must be the same as original)
delete_noredo

# delete junction, undo, but not redo (net must be the same as original)
delete_junction

# delete junction with TLS
delete_TLS

#inspect crossing
inspect

# load small net with crossings
load_small

# load big net with crossigns
load_big

