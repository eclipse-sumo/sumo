all
keepValues
removeValues

# keep elements with attributes within a given interval
keepInterval

# remove elements with attributes within a given interval
removeInterval

# remove parent elements if a child element matches the attribute value
removeValues_parents
