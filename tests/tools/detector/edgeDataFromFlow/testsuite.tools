# only flow on the main motorway, aggregate by hour
basic

# create turn-count data for detectors that have a unique successor edge
turnData

# generate cadyts output
cadyts

# no detector file use detector column as edge names
edge_names

# aggregate by hour
interval
timeColumn
time_humanReadable
timeFormat
