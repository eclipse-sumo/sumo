default

# standalone routes
standalone
restriction-output
xml-output

# only rate by mininimu distance
min-dist

# only rate by mininimum air distance
min-air-dist

# Checking for a route which visits a node twice
node_loop

# Checking for a route which visits an edge twice
edge_loop

# network with internal links
internal
