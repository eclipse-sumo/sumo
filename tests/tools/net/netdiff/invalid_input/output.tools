usage: netdiff.py [-h] [-c FILE] [-C FILE] [--save-template FILE] [-sn SOURCE]
                  [-dn DEST] [-op OUTPREFIX] [-v] [-p] [-d] [-i] [--copy COPY]
                  [--path PATH] [--remove-plain] [-l] [-s] [-g]

Compares two net.xml files

options:
  -h, --help            show this help message and exit
  -c FILE, --configuration-file FILE
                        read configuration from FILE
  -C FILE, --save-configuration FILE
                        save configuration to FILE and exit
  --save-template FILE  save configuration template to FILE and exit
  -sn SOURCE, --source-network SOURCE
                        source network
  -dn DEST, --destiny-network DEST
                        destiny network
  -op OUTPREFIX, --prefix OUTPREFIX
                        output prefix
  -v, --verbose         Give more output
  -p, --use-prefix      interpret source and dest as plain-xml prefix instead
                        of network names
  -d, --direct          compare source and dest files directly
  -i, --patch-on-import
                        generate patch that can be applied during initial
                        network import (exports additional connection
                        elements)
  --copy COPY           comma-separated list of element names to copy (if they
                        are unchanged)
  --path PATH           Path to binaries
  --remove-plain        avoid saving plain xml files of source and destination
                        networks
  -l, --write-selections
                        Write selection files for created, deleted and changed
                        elements
  -s, --write-shapes    Write shape files for created, deleted and changed
                        elements
  -g, --plain-geo       Write geo coordinates instead of network coordinates
