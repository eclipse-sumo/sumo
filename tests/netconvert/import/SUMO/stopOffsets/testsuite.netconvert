# Specifies stopOffset as child of lanes
in_lane

# Specifies stopOffset as child of some lanes and gives a default value for the edge, too
in_lane_and_edge_mixed

# Test warnings for duplicate definitions, invalid offset values, and invalid vclass names.
warnings

# Test error in case of incomplete specification (missing value).
incomplete_specification

# Specifies stopOffset as child of edge (affects all lanes)
in_edge
