# spread types
spread_types

# Tests for using laneTypes
laneTypes

# Simple cross using types for edge definition (verbose). 
# The additional --remove-edges.by-vclass should remove 
# one edge during net conversion.
cross_allow_v

# Overwriting the allowed attribute using an own (edge-) value
cross_allow_with_own_v

# Simple cross using explicite edge information
cross_notypes

# Simple cross using explicite edge information, speed is given in km/h (verbose)
cross_notypes_v

# Simple cross using types for edge definition
cross_usingtypes

# Simple cross using types for edge definition (verbose)
cross_usingtypes_v

# Simple cross disallowing ships
cross_default_disallow

# Simple cross allowing only bikes
cross_default_allow

# Simple cross using types for edge definition
cross_default_type
ignore_unknown_type

# Simple cross using types for edge definition and overwriting allowed
cross_usingtypes_overwrite

# test invalid default lanenumber and width
cross_default_invalid

# load two type files, values from the second file should overwrite the first
overwrite_type

# make sure that allow="all" is written when this is not the edge type default (#14625)
allow_all_type_disallows
routingType
