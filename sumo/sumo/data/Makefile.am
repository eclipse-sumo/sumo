# In packages with subdirectories, the top level Makefile.am must tell
# Automake which subdirectories are to be built. This is done via the SUBDIRS
# variable.
SUBDIRS = config nets routes routedefs detectors junctions

