# default values of selection
select_default

# filtering for edges that allow 'passenger' should also match edges that allow 'all' or 'all' (combined)
select_allow_all

# try to select elements with wrong parameters
wrong_parameters

# select differents busstops
selectadditional_basic

# check set operators clear and invert
select_setoperators

# check that undo-redo of elements works correctly
undo_redo
