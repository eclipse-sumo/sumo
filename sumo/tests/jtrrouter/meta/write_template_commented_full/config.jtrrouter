<configuration>

    <input
        <!-- Use FILE as SUMO-network to route on -->
        net-file=""

        <!-- Read alternatives from FILE -->
        alternatives=""

        <!-- Read network weights from FILE(s) -->
        weights=""

        <!-- Read lane-weights from FILE(s) -->
        lane-weights=""

        <!-- Name of the xml attribute which gives the edge weight -->
        measure="traveltime"

        <!-- Read flow-definitions from FILE -->
        flow-definition=""

        <!-- Read turn-definitions from FILE -->
        turn-definition=""

    />

    <output
        <!-- Write generated routes to FILE -->
        output-file=""

        <!-- Write exit times (weights) for each edge -->
        exit-times="false"

    />

    <processing
        <!-- Continue if a route could not be build -->
        continue-on-unbuild="false"

        <!-- Assume input is unsorted -->
        unsorted="false"

        <!--  -->
        randomize-flows="false"

        <!-- Prune the number of alternatives to INT -->
        max-alternatives="5"

        <!-- Emit FLOAT random vehicles per second -->
        random-per-second=""

        <!--  -->
        prune-random="false"

        <!-- Remove loops at start and end of the route -->
        remove-loops="false"

        <!-- Tries to correct a false route -->
        repair="false"

        <!--  -->
        max-edges-factor="2"

        <!-- Use STR as default turn definition -->
        turn-defaults="30,50,20"

        <!-- Use STR as sink list -->
        sinks=""

        <!--  -->
        accept-all-destinations="false"

        <!--  -->
        ignore-classes="false"

        <!-- Continue on broken input -->
        dismiss-loading-errors="false"

    />

    <defaults
        <!-- Assigns a default depart lane -->
        departlane=""

        <!-- Assigns a default depart position -->
        departpos=""

        <!-- Assigns a default depart speed -->
        departspeed=""

        <!-- Assigns a default arrival lane -->
        arrivallane=""

        <!-- Assigns a default arrival position -->
        arrivalpos=""

        <!-- Assigns a default arrival speed -->
        arrivalspeed=""

        <!-- Defaults will override given values -->
        defaults-override="false"

    />

    <time
        <!-- Defines the begin time; Previous trips will be discarded -->
        begin="0"

        <!-- Defines the end time; Later trips will be discarded -->
        end="86400"

    />

    <report
        <!-- Switches to verbose output -->
        verbose="false"

        <!-- Prints option values before processing -->
        print-options="false"

        <!-- Prints this screen -->
        help="false"

        <!-- Disables output of warnings -->
        suppress-warnings="false"

        <!-- Writes all messages to FILE -->
        log-file=""

        <!-- Defines how often statistics shall be printed -->
        stats-period="-1"

    />

    <random_number
        <!-- Initialises the random number generator with the given value -->
        srand="23423"

        <!-- Initialises the random number generator with the current system time -->
        abs-rand="false"

    />

</configuration>
