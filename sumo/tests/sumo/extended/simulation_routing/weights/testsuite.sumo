# Ordered list of tests in test suite. Add as appropriate

# test initializing the rerouting device weights with loaded edge weights
init_with_loaded_weights

# using option --device.rerouting.output
weight_output

# average over a fixed window instead of exponential average
weight_average_steps

# randomization to break symmetry
randomized_weights
