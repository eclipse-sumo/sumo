# Two programs are set; the second replaces the first (same subid)
basic_replaceAdded

# Two programs are set; the second has a different subid
basic_addTwo

# Two programs are set; both replace the original (same subid)
basic_replace

# replace with a program that has less phases (see ticket #652)
replace_less_phases
